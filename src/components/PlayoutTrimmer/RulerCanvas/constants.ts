import { theme } from '../../../theme';

export const activeBarColor = theme.palette.primary.main;
export const inactiveBarColor = theme.palette.text.secondary;

export const majorBarHeight = 16;
export const minorBarHeight = 6;
